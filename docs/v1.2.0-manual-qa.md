# Memories Pet Life v1.2.0 Manual QA

Last updated: 2026-07-01

## Build and Version

- Confirm the app builds successfully.
- Confirm `MARKETING_VERSION` is `1.2.0`.
- Confirm `CURRENT_PROJECT_VERSION` is `1`.
- Open Settings and confirm only `1.2.0` is displayed.
- Confirm the Settings screen does not display build number `(1)`.
- Confirm CloudKit entitlement was not added.

## Calendar Delete

- Open a calendar day with an existing record.
- Confirm the delete button uses a red destructive style with a trash icon.
- Tap delete and confirm an alert appears before deletion.
- Tap cancel and confirm the record remains.
- Tap delete again and confirm deletion from the alert.
- Confirm the calendar repository entry is removed.
- Confirm widget snapshot and timeline updates still occur.
- Confirm `onSaved()` and dismiss behavior still occur.
- Confirm delete cannot be tapped repeatedly while processing.

## Photo Editing

- Select a photo from Home.
- Confirm the editor opens from the "Photo / Video Editor" entry.
- Confirm ticket frame editing works.
- Confirm simple card editing works.
- Confirm retro film editing works.
- Adjust photo scale and position, then confirm preview and export match the editor.
- Save the photo to the Photos app.
- Share the photo.
- Save and reopen a photo draft.
- Delete a photo draft and confirm its image and thumbnail files are removed.
- Confirm watermark-free limits and paid pass behavior match v1.1.0.

## Video Editing

- Select a vertical video.
- Select a horizontal video.
- Confirm videos are not rejected because of duration.
- Confirm videos are not automatically trimmed.
- Confirm the editor preview loops muted.
- Confirm ticket frame editing works with video.
- Confirm the video can be scaled and moved inside the ticket photo frame.
- Confirm simple card editing works with video.
- Confirm retro film editing works with video.
- Confirm date, location, main text, sub text, icons, colors, alignment, and watermark render on video.
- Save a video with audio and confirm audio is preserved.
- Save a video without audio and confirm export succeeds.
- Share an exported video.
- Save and reopen a video draft.
- Delete a video draft and confirm its video and thumbnail files are removed.
- Confirm watermark-free limits and paid pass behavior also apply to videos.

## Storage Cleanup

- Select a video, open preview, export, then leave the preview screen.
- Confirm preview/share temporary files do not remain indefinitely.
- Open Settings and run "Delete Temporary Files".
- Confirm temporary edit/export files are removed.
- Confirm saved drafts are not deleted by temporary cleanup.
- Relaunch the app and confirm orphan draft cleanup does not remove valid drafts.
- Confirm app-created temporary and draft media files are excluded from iCloud backup.

## Announcements

- Confirm Home shows a bell icon.
- Use a test/cached JSON response with one active visible announcement.
- Confirm the unread badge appears.
- Open the announcements list.
- Confirm unread/known-read styling is visible.
- Open an announcement and confirm it is marked read.
- Use "Mark All as Read" and confirm the badge clears.
- Confirm announcements with URLs show "Learn More".
- Confirm URLs open in Safari presentation.
- Disable network or force fetch failure and confirm cached announcements remain visible.
- Confirm inactive announcements are hidden.
- Confirm `minimumAppVersion` and `maximumAppVersion` filtering work.
- Confirm old update announcements published before the local display baseline are hidden for new users when `showForNewUsers` is `false`.
- Confirm important announcements published before the local display baseline remain visible when `showForNewUsers` is `true`.
- Confirm announcements past `expiresAt` are hidden from the list.
- Confirm announcements past `expiresAt` are not included in the unread badge.
- Confirm announcements hidden by `showForNewUsers: false` are not included in the unread badge.
- Confirm the unread badge counts only announcements that are currently visible and unread.
- Confirm existing read/unread management still works after the new filters.
- Confirm cached announcements use the same `expiresAt` and `showForNewUsers` filters.
- Confirm Japanese and English title/body fallback works.
- Confirm the app repository does not contain a production `announcements.json` file.

## Review Request

- Confirm no review request is attempted on first launch.
- Confirm no review request is attempted just by opening the editor.
- Confirm no review request is attempted after failed saves.
- Confirm no review request is attempted after draft saves.
- Confirm no review request is attempted after share-only actions.
- Confirm successful photo save increments the save-success count.
- Confirm successful video save increments the save-success count.
- Confirm successful calendar image save increments the save-success count.
- Confirm the first review request candidate appears only after the configured successful-save threshold.
- Confirm another request is not attempted within 50 days.
- Confirm another request is not attempted twice for the same app version.

## App Store Review Notes

Use the following English note in App Store Connect if helpful:

```text
v1.2.0 adds photo/video editing support, in-app announcements loaded from our public JSON endpoint, improved calendar delete confirmation, dynamic version display, and a review request flow after successful saves.

The announcement JSON is used only to display app news in the bell icon screen. It does not require login and does not collect or upload user photos, videos, edits, drafts, identifiers, or user-generated content.
```

Japanese internal note:

```text
v1.2.0では、写真/動画編集対応、公開JSONから取得するアプリ内お知らせ、カレンダー削除確認、設定画面のバージョン表示修正、保存成功後のレビュー依頼導線を追加しました。

お知らせJSONはホーム画面のベルアイコン内でアプリからのお知らせを表示するためだけに使用します。ログインは不要で、ユーザーの写真、動画、編集内容、下書き、識別子、ユーザー生成コンテンツを収集・アップロードしません。
```
